@using JoinRpg.Web.Models.Plot
@model EditPlotFolderViewModel

<div class="modal fade" id="restoreElementModal" tabindex="-1" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            @using (Html.BeginForm("UnPublishElement", "Plot", new { Model.PlotFolderId, Model.ProjectId }, FormMethod.Post))
            {
                <input type="hidden" name="PlotElementId" id="publishPlotElementId" value="0" />
                @Html.AntiForgeryToken()
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Восстановить вводную</h4>
                </div>

                <div class="modal-body">Вводная будет восстановлена и скрыта от игроков</div>

                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">Восстановить</button>
                    <button type="button" class="btn btn-default" data-dismiss="modal">Отмена</button>

                </div>
            }
        </div>
    </div>
</div>
