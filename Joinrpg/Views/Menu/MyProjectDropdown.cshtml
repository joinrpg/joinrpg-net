@using JoinRpg.DataModel
@model System.Collections.Generic.IEnumerable<JoinRpg.DataModel.Project>

@helper ShowProjectItem(Project project)
{
    <li>@Html.ActionLink(@project.ProjectName, "Details", "Game", new {project.ProjectId}, null)</li>
}

@if (Model.Count() > 1)
{
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Мои проекты <span class="caret"></span></a>

    <ul class="dropdown-menu">
        @foreach (var project in @Model)
        {
            @ShowProjectItem(project)
        }
    </ul>
</li>
}
else if (Model.Count() == 1)
{
    @ShowProjectItem(Model.Single())
}