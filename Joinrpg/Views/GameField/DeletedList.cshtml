@using JoinRpg.Web.Models
@model GameFieldListViewModel

@{
    ViewBag.Title = "Поля персонажа / заявки";
}

<div>
    <h2>@ViewBag.Title</h2>
    <ul class="nav nav-tabs">
        <li><a href="@Url.Action("Index", new {Model.ProjectId})">Активные</a></li>
        <li class="active"><a href="#">Удаленные</a></li>
    </ul>
    <div class="join alert alert-warning" style="margin-top: 1em;"><i class="glyphicon glyphicon-info-sign"></i><div>Здесь показываются только поля, которые были использованы хотя бы один раз.</div></div>    
</div>

@Html.Partial("FieldList", Model.Items.Where(i => !i.IsActive))
