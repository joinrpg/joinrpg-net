apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: joinrpg-portal
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - dummy-joinrpb.ru
      secretName: dummy-joinrpb-ru-tls
  rules:
    - host: dummy-joinrpb.ru
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: joinrpg-portal
                port:
                  number: 80