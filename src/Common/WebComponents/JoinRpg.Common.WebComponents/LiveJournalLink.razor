@if (Contact is not null)
{
    <ContactLink Prefix="ЖЖ:" Href="@($"https://{Contact.Value}.lj.ru")">@Contact.Value</ContactLink>
}

@code {
    [Parameter]
    public LiveJournalId? Contact { get; set; }
}
