@{
    var effectiveSegment = Link ?? (Contact is null ? null : $"id{Contact.Id}");
}
@if (effectiveSegment is not null)
{
    <ContactLink Prefix="ВК:" Href="@($"https://vk.com/{effectiveSegment}")">@effectiveSegment</ContactLink>
}

@code {
    [Parameter]
    public VkSocialLink? Contact { get; set; }
    [Parameter]
    public string? Link { get; set; }
}
