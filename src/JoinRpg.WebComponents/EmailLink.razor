@if (Contact is not null)
{
    <ContactLink IconClass="glyphicon-envelope" Href="@($"mailto:{Contact.Value}")">@Contact.Value.Trim()</ContactLink>
}

@code {
    [Parameter]
    public Email? Contact { get; set; }
}
