@switch (Status)
{
    case MandatoryStatusViewType.Optional:
        break;
    case MandatoryStatusViewType.Recommended:
        <JoinIcon Icon="JoinIconType.RecommendedStar" Title="Рекомендуется заполнить это поле" />
        break;
    case MandatoryStatusViewType.Required:
        <JoinIcon Icon="JoinIconType.MandatoryStar" Title="Поле обязательно к заполнению" />
        break;
    default:
        throw new ArgumentOutOfRangeException();
}

@code {

    [Parameter]
    [EditorRequired]
    public MandatoryStatusViewType Status { get; set; }

}
