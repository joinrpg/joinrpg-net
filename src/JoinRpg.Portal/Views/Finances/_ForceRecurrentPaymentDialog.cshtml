@model JoinRpg.Web.Models.RecurrentPaymentViewModel

<div class="modal fade" id="forceRecurrentPaymentDialog" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      @using (Html.BeginForm("ForceRecurrentPayment", "Payments", FormMethod.Post, new { enctype = "multipart/form-data" }))
      {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ProjectId)
        @Html.HiddenFor(model => model.ClaimId)
        @Html.HiddenFor(model => model.RecurrentPaymentId, new { id = "forcePaymentId" })

        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="deleteTitle">Принудительный платеж</h4>
        </div>
        <div class="modal-body">
          <div class="join alert alert-warning">
            <i class="glyphicon glyphicon-alert"></i>
            <div>
              <p>После нажатия «Провести платеж» он будет совершен по выбранной подписке принудительно,
                даже если уже было проведено автоматическое списание средств.</p>
              <p>Если вы <b>не</b> уверены в том, что делаете, АСТАНАВИТЕС!!!111</p>
            </div>
          </div>
        </div>
        <div class="modal-footer" style="display: flex; flex-direction: row; align-items: center;">
          <button type="button" class="btn btn-default" style="margin-right: auto;" data-dismiss="modal">Отмена</button>
          <button type="submit" class="btn btn-warning">Совершить платеж</button>
        </div>
      }
    </div>
  </div>
</div>
