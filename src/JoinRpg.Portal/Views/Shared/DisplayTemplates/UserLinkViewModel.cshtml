@model JoinRpg.Common.WebComponents.UserLinkViewModel
@if (Model == null)
{
    <span>нет</span>
    return;
}
<component
  type="typeof(JoinRpg.Common.WebComponents.UserLink)"
  render-mode="Static"
  param-Model="@Model" />
