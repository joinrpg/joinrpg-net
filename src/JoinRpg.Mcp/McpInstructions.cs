namespace JoinRpg.Mcp;

/// <summary>
/// Текст для <c>ServerInstructions</c> при <c>initialize</c> (ADR012 §7). Намеренно на русском —
/// исключение из правила локализации (structure.md): это читает модель, а не пользователь, и
/// предметная область целиком русскоязычная.
/// </summary>
internal static class McpInstructions
{
    public const string Text = """
        Это MCP-сервер joinrpg.ru — сайта для организации ролевых игр живого действия (LARP).
        Мастер игры (организатор) через тебя работает с базой своего проекта: персонажами,
        группами, полями.

        Порядок работы:
        1. list_projects — какие проекты доступны.
        2. get_project_overview по конкретному проекту — даёт дерево групп персонажей и полный
           список полей с вариантами значений. У каждого поля и у каждого варианта значения может
           быть своя спецгруппа персонажей (SpecialGroupId / CharacterGroupId у варианта) — в неё
           персонаж попадает автоматически при заполнении поля, добавить туда явно нельзя.
        3. Дальше — list_characters по группе (в том числе по спецгруппе варианта: например,
           «кто относится к партии синих» — это list_characters по группе варианта «синие», а не
           выгрузка всех персонажей с фильтрацией) или search_characters по имени.
        4. get_characters — пачкой по отобранным id. Не вызывай его по одному персонажу и не
           выгружай всех персонажей проекта, если можно сузить через группу или поиск.

        Значения полей и id групп всегда бери из get_project_overview — не выдумывай их.

        Внутренние ссылки в markdown-описаниях персонажей и полей: %персонаж123, %группа45 —
        такой текст можно показывать пользователю как есть, это принятый на сайте формат ссылок.

        Доступны только те проекты и данные, к которым пользователь дал согласие при подключении.
        Если инструмент отказал из-за проекта вне согласия — сообщи об этом пользователю, а не
        пытайся обойти ограничение.
        """;
}
