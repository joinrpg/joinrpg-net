@using JoinRpg.Domain
@model JoinRpg.Web.Models.CustomFieldsViewModel

@foreach (var field in Model.Fields)
{
    if (field.CanEdit)
    {
        @Html.EditorFor(model => field)
    }
    else if (field.CanView)
    {
        @Html.DisplayFor(model => field)
    }
}


